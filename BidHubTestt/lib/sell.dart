import 'package:flutter/material.dart';

class SellPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Sell'),
      ),
      body: Center(
        child: Text('This is the sell page'),
      ),
    );
  }
}
